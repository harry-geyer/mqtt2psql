[Unit]
Description=MQTT to PSQL Listener Service
After=network.target

[Service]
Type=notify
ExecStart=/usr/bin/mqtt2psql -c /etc/mqtt2psql/mqtt2psql.conf
Restart=on-failure
User=mqtt2psql
NotifyAccess=all

[Install]
WantedBy=multi-user.target
